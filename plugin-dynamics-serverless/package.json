{
  "name": "plugin-dynamics-serverless",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "adal-node": "^0.2.1",
    "dynamics-web-api": "^1.6.14",
    "jlafer-twilio-runtime-util": "0.0.9"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
